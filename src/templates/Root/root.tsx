import Main from '../../components/Main/main';
import SlateTemplate from '../Slate/slate';

const Root = (): JSX.Element => (
  <SlateTemplate>
    <Main />
  </SlateTemplate>
);

export default Root;
